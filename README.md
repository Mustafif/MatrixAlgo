## Steps 

```
# clone the directory 
git clone git@github.com:Mustafif/MatrixAlgo.git

# add the files with changes 
git add -A
# commit the changes to staging 
git commit -m "message"
# push the changes to the remote repository
git push 

# i make changes you want to pull
git pull 
```